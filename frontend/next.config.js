/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["limitlesstcg.nyc3.digitaloceanspaces.com", "archives.bulbagarden.net", "upload.wikimedia.org"],
      },
}

module.exports = nextConfig
